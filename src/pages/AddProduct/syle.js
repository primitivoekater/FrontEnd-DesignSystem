import { TextField, styled, FormControl, } from "@mui/material";

const CustomTextField = styled(TextField)({
    marginTop: 32
});


const CustomFormControl = styled(FormControl)({
    marginTop: 32,
    width: 190
});

export { CustomFormControl, CustomTextField }


